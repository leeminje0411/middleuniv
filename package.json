{
  "name": "cau-probe-runtime",
  "private": true,
  "workspaces": [
    "web",
    "worker"
  ],
  "scripts": {
    "dev:web": "npm --workspace web run dev",
    "start:web": "npm --workspace web run start",
    "start:worker": "npm --workspace worker run start"
  }
}
